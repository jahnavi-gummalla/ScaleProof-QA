apiVersion: 1

datasources:
  - name: ScaleProof Prometheus
    uid: scaleproof-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false